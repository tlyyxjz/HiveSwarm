# HiveSwarm 仓库体检报告

**日期**：2026-09-14 ｜ **分支**：feature/glm-t2x-experiment-machine（全量实测）｜ **性质**：只诊断不删改

## 一、快速开始实测（陌生人 3 分钟视角）

| 项 | 结果 | 详情 |
|---|---|---|
| `pip install pydantic litellm fastapi uvicorn httpx` | ⚠️ 部分可用 | 能跑通核心链路；但 gradio（面板）/reportlab（PDF 报告）未列入,功能缺半截 |
| `python -m pytest --tb=short -q` | ✅ 可跑但数字过期 | 实测 **476 passed, 1 skipped**（README 写 230） |
| `python -m src.main "帮我做一个 PPT"` | ✅ 可跑 | mock 兜底 4/4 子任务通过,无 key 也能演示 |
| 路径 | ❌ 陌生人卡点 | README 写死 `C:/Users/Lenovo/Desktop/hiveswarm`（本机私有路径且已过期） |

**结论**：命令链路本身能跑通,但 ①数字过期 ②路径写死 ③依赖清单不完整。T3.2 重写 README 时以上三条必改。

## 二、质量门禁实测（真实数字）

| 门禁 | 实测结果 | 备注 |
|---|---|---|
| ruff check | **347 错**（265 可 `--fix` 自动修） | 前三名: unused-import 102 / unsorted-imports 67 / missing-newline 55 / unused-variable 27 |
| mypy (strict) | **未运行** | 当前 Python 环境未装 mypy（pyproject 声明了 strict）——装上之前的任何"mypy 通过"说法都不许写 |
| pytest | **476 passed, 1 skipped**, 零 warning（filterwarnings=error 生效） | 1 skip 为手动联网用例 |
| 覆盖率 | **core+layers 91%；stub 83%；合计 83%** | 新增 contract/ 模块 96–100% |

**覆盖率最低 5 个模块**（T3.3 补测目标）：
1. `stub/llm_litellm.py` 46%
2. `stub/llm_providers.py` 47%
3. `stub/dashboard_gradio.py` 54%
4. `layers/monitor/logger.py` 59%
5. `layers/work/skill_registry.py` 70%

## 三、僵尸文件清单（**只列不删,待人工确认**）

| 路径 | 判断 | 建议 |
|---|---|---|
| `backup/` | 历史备份 | 建议移出仓库（已有桌面备份 20260913） |
| `hiveswarm-pro.zip` | 打包产物 | 移出仓库（产物不入库） |
| `output.pptx` | 生成物示例 | 移出,留 README 截图即可 |
| `hiveswarm-tabs/` | 12 张 UI 渲染截图 | **保留**——方案预览页的视觉参考（路线图一.4 引用） |
| `test-runtime/` | 运行时残留 | 确认后删 |
| `_demo_run.py` / `tab_screenshots.py` | 顶层脚本 | 移入 `examples/` 或 `scripts/` |
| `截图/`、`报告/`、`文档/` | 中文资料目录 | 保留但移入 `docs/` 统一收纳 |
| `src/` 与 `layers/` 双入口 | `src/main.py` 为演示入口 | 保留,README 里说明分工 |

## 四、.gitignore

基础项齐全（Python/测试/虚拟环境/IDE）。缺：`*.log`、`output/`、`experiments/runs/`（实验轨迹 1221 个 JSON 不应入库,建议忽略 runs 只留 report 模板）。

## 五、处置优先级

1. **必改（对外前）**：README 路径/数字/依赖三条（T3.2 一起做）
2. **建议做**：`ruff check --fix`（265 条自动修,一次 commit 搞定）；`.gitignore` 补三项
3. **等确认**：僵尸文件处置（上表）；mypy 安装后补跑一次留档

## 六、处置结果（2026-09-14 晚更新）

| 项 | 状态 |
|---|---|
| 优先级 1（README 三条） | ✅ 已完成（T3.2,中英双语重写,快速开始命令全部实测复跑） |
| 优先级 2（ruff 自动修） | ✅ 已完成：347 → 82 错（265 自动修,476→513 测试全绿） |
| 优先级 2（.gitignore） | ✅ 已完成；并发现**实验轨迹 1222 文件被误提交**,已移出版本控制 |
| T3.3 覆盖率 | ✅ 已完成：logger 59→83%、skill_registry 70→82%、llm_litellm 46→86%、llm_providers 47→76%、dashboard 54→61%（余量为 Gradio 声明式布局与真实 HTTP 调用体,继续测属于刷数字,按标准 4.5 停手）；总覆盖 83%→**87%** |
| 优先级 3（僵尸文件/mypy） | ⏸ 仍等人工确认/mypy 安装 |
| 定价区 | ⚠️ README 重写时移除了 Gumroad 定价区——**需人工拍板**是否加回（对外门面定位:评委/面试官/开源用户 vs 付费转化） |
