# 任务单（补录）：T1.2 / T1.3 / T1.4

> 三张任务单合录一文件（同一晚连续执行、分支链式叠加）。负责人：GLM-5.3-Flash；人工负责人：Sol。
> 共同输入：docs/榫卯改造方案.md 步骤 2/3/4；设计稿 4.2/4.3。共同禁止修改：现有测试断言、layers/work/pool.py、pyproject（无新依赖）。共同验收：新增单测全绿 + 全量 unit 回归不红 + 变异测试全被抓。

## T1.2 反向可达搜索 + 污染锥
- **目标**：被证伪断言 → 确定性图搜索定位"最早被证伪"（非报错位置）；污染锥标记不可信下游；只重跑集合。
- **允许修改**：layers/contract/causal.py（新）、tests/unit/test_contract_causal.py（新）。
- **接口**：`find_root_cause(ledger, symptom_aid) -> RootCause(aid, distance, path) | None`；`contamination_cone` / `rerun_set`。环路安全，平局取 aid 字典序。
- **验收硬指标**：≥5 步依赖链、第 2 步被证伪、第 5 步报错 → 必须返回第 2 步（任务包原文）。
- **非目标**：dispatch（T1.3）、执行端接线。
- **结果**：13 单测全绿；变异 2 个（过滤证伪状态/锥不扩散）全被抓。commit 5ee2dbc。

## T1.3 类型驱动 dispatch + 回归闸（核心创新）
- **目标**：被证伪断言的 (kind, 证伪类型) → 唯一结构动作，替代关键词猜测；回归闸拦"修 A 坏 B"。
- **允许修改**：layers/repair/{strategy_table,regression_gate,typed_fixer}.py、tests/unit/test_repair_dispatch.py（新）。
- **接口**：`TypedDispatch.dispatch/from_falsified`、`DISPATCH_TABLE`（12 格完备）、`RegressionGate.gate/rollback`、`plan_for_symptom`。旧 StrategyTable 路径与测试原样保留（方案 R3）。
- **设计决定**：pre+threshold→swap_adapter、post+value→re_observe、post+existence→re_assemble 为类型完备性补全的格子（设计稿未写死，理由：pre 阈值错=输入形状问题走翻译层；post 值错先重取证不轻易动装配；post 存在错=意图落空重组）。
- **结果**：25 单测全绿（含旧 repair 测试）；变异 2 个全被抓——**变异 1 曾漏抓，暴露测试缺 post+value 语义断言，补 12 格锁死后重做全被抓**。相关 commit 5ee2dbc 后续（d7dd0fc 误提交旧版 strategy_table，已用 fix commit 恢复并记录教训：未提交内容禁用 git checkout 恢复）。

## T1.4 验证阶梯
- **目标**：L1 连续 N≥3 次稳定通过 → 自动升 L2（事前拦截）；证伪 → 立刻降回 L1；全程事件可审计。
- **允许修改**：layers/contract/escalation.py（新）、layers/contract/__init__.py、tests/unit/test_contract_escalation.py（新）。
- **接口**：`VerificationLadder(ledger, promote_after=3, emit).observe(aid, ok, ts)` → ObservationOutcome；事件 ASSERTION_ESCALATED/DEGRADED/FALSIFIED。L0 跳过；L3 不自动升降、证伪时降 L1；不碰 pool._tick_health 与 monitor（任务包约束）。
- **结果**：9 单测全绿（含 10 轮主线场景）；变异 2 个（阈值失效/证伪不降级）全被抓。
