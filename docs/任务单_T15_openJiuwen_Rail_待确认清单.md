# 任务单：T1.5 openJiuwen Rail 适配层 —— **阻塞：待 API 资料确认**

**状态**：未实施。任务包明令"如果 openJiuwen 的具体 API 你不确定，先告诉我，不要编造 API"。
本文件列出实施前必须由人工确认的全部假设；**资料到位后预计 1 个任务轮次完成**。

## 需要人工提供的资料（任一形式：文档 URL / 仓库路径 / 示例代码）

1. **Rail 的注册入口**：扩展点叫什么（`register_rail`? hook 注册表? 配置文件?）、在进程生命周期的哪个时机调用（启动? 每任务?）
2. **能力挂钩子的签名**：钩子能拿到什么上下文（任务描述? 工具调用参数? 中间结果?），能返回什么（放行/拦截/改写?）
3. **优先级语义**：多条 Rail 时的执行顺序、短路规则（高优先级拦截后低优先级还跑吗）
4. **openJiuwen 的"工具调用"模型**：它有没有类似 SkillPool 的能力层？榫卯三机制要拦的两个点（调用前=事前拦截 L2 / 结果返回后=事后检查 L1）分别挂在哪
5. **内核边界**：哪些模块禁止修改（用于证明"不改内核"）

## 我方将提供的设计（已定，不依赖上述答案）

- 适配层形态：`integrations/openjiuwen_rail/rail.py`，把 M1 编译器+账本 / M2 dispatch+回归闸 / M3 阶梯包成一条 Rail
- 挂载点映射：事前拦截 → Rail 的 pre-call 钩子（L2 断言在此执行）；事后检查 → post-call 钩子（L1 断言 + 证伪记账 + 反向搜索在此触发）
- 验收：openJiuwen 实例上跑通一个最小任务，三机制的拦截/修复各触发一次，贴原始输出
