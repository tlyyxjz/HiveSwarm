[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hiveswarm-skill-ppt"
version = "0.1.0"
description = "PPT 4-step pipeline skill pack for HiveSwarm"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]